import { ProductFilterPricePipe } from './product-filter-price.pipe';

describe('ProductFilterPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterPricePipe();
    expect(pipe).toBeTruthy();
  });
});
